/// Application layer: the `PurificationEngine` stream processor.
pub mod application;
/// Domain layer: the `Purifier` trait and built-in purifier implementations.
pub mod domain;
/// Infrastructure layer: CLI definitions and I/O helpers.
pub mod infra;
